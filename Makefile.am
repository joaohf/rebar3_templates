AUTOMAKE_OPTIONS = foreign

if STUBBED_MODE
export STUBBED_MODE=true
endif

# stage directory where Erlang Port drivers will be installed
export DESTDIR = $(abs_builddir)/_build/stage

SUBDIRS = c_src